server {
    listen 80 default_server;
    server_name _;  # Accept any host

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback for Vue Router
    location / {
        try_files $uri /index.html;
    }

    # Optional: allow large requests
    client_max_body_size 10M;
}